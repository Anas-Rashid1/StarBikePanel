import React from 'react'

const Input = ({type, placeholder}) => {
  return (
    <Input type={type} placeholder={placeholder} className=" p-4 bg-gray-300 text-gray-700 shadow-lg shadow-slate-700"/>
  )
}

export default Input